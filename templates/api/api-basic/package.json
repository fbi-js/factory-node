{
  "name": "<%= project.name %>",
  "version": "0.0.0",
  "description": "<%= project.description %>",
  "main": "/lib/index.js",
  "scripts": {
    "dev": "fbi s",
    "debug": "fbi s -d",
    "prod": "cross-env NODE_ENV=production node dist/app",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mrapi/api": "^1.0.0",
    "cross-env": "^7.0.2"
  }
}
